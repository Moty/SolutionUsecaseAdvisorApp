---
applications:
  - name: solution-usecase-advisor
    memory: 1000M
    instances: 1
    buildpack: nodejs_buildpack
    path: .
    command: npm start
    random-route: false
    env:
      NODE_ENV: production
      NPM_CONFIG_PRODUCTION: true
    # Optional service bindings (uncomment and adjust if needed)
    # services:
    #   - my-mongodb-service
    #   - my-logging-service
